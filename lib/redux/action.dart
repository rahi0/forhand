// ALL ACTIONS CLASSES.

class SingleDivMatch{ // add my matches initial data
    List myMatches; 
    SingleDivMatch(this.myMatches);
    
}
class DivisionData{ // add my matches initial data
    List match; 
    DivisionData(this.match);
}
class DivisionRank{ // add my matches initial data
    List divRank; 
    DivisionRank(this.divRank);
}


class UpdateDivResult{ // update the div result by id
  List myMatches; 
  UpdateDivResult(this.myMatches);
}

class BottomNavigationChild{ 
    int bottomNavigation; 
   BottomNavigationChild(this.bottomNavigation);
}
class AmricanoMatches {
  List amricanoMatches; 
  AmricanoMatches(this.amricanoMatches);
}
class UpdateAmericanoResult {
  List amricanoMatches; 
  UpdateAmericanoResult(this.amricanoMatches);
}
class AmericanoRanks {
  var americanoRanks; 
  AmericanoRanks(this.americanoRanks);
}
class HomePageData {
  var homePageData; 
  HomePageData(this.homePageData);
}
class TournamentData {
  var tournamentData; 
  TournamentData(this.tournamentData);
}
class ChangeBool {
  int type;
  bool option;  
  ChangeBool(this.type, this.option);
}














class DemoAction{
  var demoAction;
  DemoAction(this.demoAction);
}
